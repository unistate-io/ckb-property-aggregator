{
  "name": "nervos-dao-server",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "docker": "docker build --pull --no-cache -t ckb-property-aggregator ."
  },
  "dependencies": {
    "@spore-sdk/core": "^0.1.0",
    "bun": "^1.1.42"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
